process {
    executor = 'local'
    queue = 'all.q'
    clusterOptions = '-S /bin/bash'
}

env {
    PATH = "$PWD/scripts:$PWD/external/a5/bin:$PWD/external/art:$PWD/external/last:$PWD/external/sgevolver:$PWD/external/bedtools:$PATH"
}

